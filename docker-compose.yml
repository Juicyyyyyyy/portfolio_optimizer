services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
    # Reload command for development
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
